const account = require('./Account')
const order = require('./Order')
const products = require('./Products')
const types = require('./Types')
const typegroups = require('./Typegroups')
const password = require('./Password')

module.exports = {
  account,
  order,
  password,
  products,
  types,
  typegroups
}
